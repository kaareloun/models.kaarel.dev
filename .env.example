# Telegram notifications for new Pareto frontier models (src/scripts/paretoWatch.ts)
# Token from @BotFather — enables notifications + /start subscriptions
TELEGRAM_BOT_TOKEN=

# Notifications are sent to every chat that has messaged the bot (/start).
# The bot username shown in the site footer is hardcoded in src/openrouter/constants.ts.
